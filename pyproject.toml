[project]
name = "spur-content-telemetry-conformance"
version = "0.0.0"
description = "Locked environment for the Content Telemetry conformance suite"
requires-python = "==3.12.*"
dependencies = [
  "jsonschema==4.26.0",
  "rfc8785==0.1.4",
]

[tool.uv]
package = false
